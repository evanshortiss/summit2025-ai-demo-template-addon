---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pipeline
  labels:
    {{- include "langchain-agent-build.labels" . | nindent 4 }}
imagePullSecrets:
  - name: {{ .Values.app.name }}-registry-creds