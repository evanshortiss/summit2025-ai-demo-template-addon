---
apiVersion: v1
kind: Secret
metadata:
  name: ${{ values.name }}-repo
  namespace: openshift-gitops
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  url: https://${{ values.host }}/${{ values.owner }}/${{ values.destination }}.git